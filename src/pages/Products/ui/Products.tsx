import {PageTemplate} from "@/shared";
import {SectionProducts} from "@/pages/Products/ui/sections/SectionProducts.tsx";

export const ProductsPage = () => {
    return (
        <PageTemplate>
            <SectionProducts  />
        </PageTemplate>
    )
}